# snowpack-instability-prediction

Two-stage ML pipeline predicting avalanche problem types and danger levels from weather sequences and physics-informed features, served via FastAPI. Phases: data foundation, RF baseline, LSTM sequence model, API deployment, spatial downscaling for micro-weather in complex terrain.
